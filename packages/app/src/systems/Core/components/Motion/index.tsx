import { Box } from '@fuel-ui/react';
import { motion } from 'framer-motion';

export const MotionStack = motion(Box.Stack);
export const MotionFlex = motion(Box.Flex);
export const MotionBox = motion(Box);
