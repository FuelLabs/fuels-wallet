import { cssObj } from '@fuel-ui/css';

export const styles = {
  detailItem: cssObj({
    padding: '$3 $6',
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    display: 'flex',
  }),
  text: cssObj({
    fontSize: '$sm',
    fontWeight: '$normal',
  }),
};
